using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelHandlerScriptLoader : MonoBehaviour
{
    /*
    [SerializeField] SliderToHeightOffset _sliderToHeightOffset;

    public void LoadLevelHandler()
    {
        _sliderToHeightOffset.currLevelHandler = WorldHandler.Instance.GetCurrLevelHandler();
    }
    */
}
